node_modules/
# dist/ is committed: the MCP server runs `node dist/index.js` from a checkout,
# so the build artifact must be present even on a fresh clone / after git clean.
.env
.env.local
.env.*.local
*.log
.DS_Store
.omc/
.claude/
coverage/
